name: taskly
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.30.0
  firebase_auth: ^4.17.8
  cloud_firestore: ^4.15.8
  firebase_storage: ^11.7.5
  firebase_messaging: ^14.7.12

  # Web (opcional se for usar Web)
  firebase_core_web: ^2.17.0
  firebase_auth_web: ^5.7.5
  cloud_firestore_web: ^3.10.5
  firebase_storage_web: ^3.6.6
  firebase_messaging_web: ^3.7.5

  provider: ^6.1.2
  flutter_svg: ^2.0.10+1
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_lints: ^3.0.1
  flutter_test:
    sdk: flutter
